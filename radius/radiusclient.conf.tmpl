authserver      ${RADIUS_SERVER}:${RADIUS_AUTH_PORT}
acctserver      ${RADIUS_SERVER}:${RADIUS_ACCT_PORT}
servers         /etc/radcli/servers
dictionary      /usr/share/radcli/dictionary
login_radius    /usr/sbin/login.radius
seqfile         /var/run/radcli/radius.seq
mapfile         /etc/radcli/port-id-map
default_realm
radius_timeout  10
radius_retries  3
nas_identifier  ${RADIUS_NAS_IDENTIFIER}
